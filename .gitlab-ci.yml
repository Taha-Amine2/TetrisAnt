stages:
  - build
  - notify

variables:
    DOCKER_DRIVER: overlay2 

build:
  stage: build
  image: docker:kaniko
  services:
     - docker:dood
  script:
    - docker build -t TetrisAnt:latest .
  artifacts:
    paths:
      - bin/Tetris.jar
  tags:
    - dosi
