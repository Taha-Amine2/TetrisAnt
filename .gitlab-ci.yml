stages:
  - build

build:
  stage: build
  script:
    - docker build -t tetrisant:latest .
    - docker run -d --name conteneur testrisant
    - docker cp conteneur:/app/dist/Tetris.jar bin/Tetris.jar

  artifacts:
    paths:
      - bin/Tetris.jar
  tags:
    - dosi

